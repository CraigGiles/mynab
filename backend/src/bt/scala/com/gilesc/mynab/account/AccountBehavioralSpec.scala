// package com.gilesc
// package mynab
// package account

// import com.gilesc.mynab.user._

// class AccountBehavioralSpec extends TestCase {
//   import AccountGroupService._
//   val userId = UserId(1L)

//   behavior of "Accounts"
//   it should "persist the account in a database" in {
//     println(userId)
//     // val before = AccountRepository.count()
//     // val id = before + 1L
//     // val aType = Banking
//     // val name = "newaccount"
//     // val expected = Account.create(id, aType, name)
//     // val ctx = AccountService.CreateRequest(userId, groupId, name, aType)

//     // AccountService.createWithPersistence(userId, name) should be(Right(expected))
//     // AccountRepository.count() should be(id)
//   }
// }
